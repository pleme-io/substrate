# This file was @generated by crate2nix 99.0.0 with the command:
#   "generate"
#
# FIXTURE — a hypothetical future crate2nix whose `version` no longer
# follows `crateName` on the next line. The tie must report a PARSER GAP,
# naming this file, and must NOT report it as staleness: sending an
# operator to regenerate an artifact that is already current is a worse
# failure than not checking, because it looks like a real finding.
{ pkgs ? import <nixpkgs> { } }:

{
  internal.crates = {
    "fixture-root" = {
        crateName = "fixture-root";
        edition = "2021";
        authors = [ "nobody" ];
        version = "0.1.0";
    };
  };
}
